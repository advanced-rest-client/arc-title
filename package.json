{
  "name": "arc-title",
  "version": "0.0.1",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "An element that controls ARC's main title element depending on route params",
  "devDependencies": {
    "polymer-cli": "^1.2.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": false,
  "main": [
    "arc-title.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "lint": "node_modules/.bin/polymer lint arc-title.html --rules polymer-1",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"arc-title:local-test\"",
    "test": "node_modules/.bin/polymer test --plugin local",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/arc-title.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/arc-title/issues",
    "email": "arc@mulesoft.com"
  }
}
